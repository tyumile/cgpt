# API App

FastAPI backend for Stage 1 chat MVP.

## Run (inside compose)
- API auto-starts with `uvicorn app.main:app`.

## Local dev (optional)
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
